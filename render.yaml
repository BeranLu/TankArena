services:
  - type: web
    name: tank-arena
    runtime: node
    region: frankfurt
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm run start
    autoDeploy: true
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: 24.16.0
      - key: ADMIN_PASSWORD
        sync: false
  - type: web
    name: tank-arena-staging
    branch: staging
    runtime: node
    region: frankfurt
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm run start
    autoDeploy: true
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: 24.16.0
      - key: ADMIN_PASSWORD
        sync: false
